<% if Setting.plugin_redmine_cas[:enabled] %>

<% content_for :header_tags do %>
  <%= stylesheet_link_tag "login", :plugin => "redmine_cas" %>
<% end %>

<div id="cas-login">
  <p style="text-align:center; margin-bottom:60px;">
  <strong><%= link_to(Setting.plugin_redmine_cas[:cas_button_text], :controller => "account", :action => "cas", :ref=>URI(params['back_url']).path) %></strong>
  </p>
<h2  style="text-align:center;"><%= Setting.plugin_redmine_cas[:cas_between_text] %></h2>
</div>
<% end %>
